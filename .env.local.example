NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: enables real AI triage. Without this, the app returns a safe mock response.
OPENAI_API_KEY=

# Dev-only helpers (do NOT use in production)
# When set to 1, the signup page will call a dev API to auto-confirm new users
NEXT_PUBLIC_DEV_AUTOCONFIRM=
# Required only for dev auto-confirm API to work (server-side only, never exposed)
SUPABASE_SERVICE_ROLE_KEY=
